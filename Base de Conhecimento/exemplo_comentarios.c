#include <stdio.h>

int main() {
	
	//Comentário simples - 1 única linha
	printf("Mensagem qualquer (aperte uma tecla para encerrar)...");
	
	/*
	 * Bloco de comentário, iniciado por barra asterisco e finalizado por asterisco barra
	 * Dentro desse bloco, qualquer coisa escrita não será interpretada pelo compilador
	 * 
	 * Esperando algum comando vindo do teclado com o getchar
	 */
	getchar();
	
	return 0;
}
